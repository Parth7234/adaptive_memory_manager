# Context-Aware Adaptive Memory Management System
# Pin to CPU-only PyTorch for smaller Docker image (~800MB vs ~4GB with CUDA)
# The simulation runs fine on CPU; MPS/CUDA are optional for faster training

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0
tabulate>=0.9.0
onnx>=1.15.0
onnxscript>=0.1.0
